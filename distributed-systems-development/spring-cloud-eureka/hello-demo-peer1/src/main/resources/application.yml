server:
  port: 8001
spring:
  application:
    name: helloserver



eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:9000/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
